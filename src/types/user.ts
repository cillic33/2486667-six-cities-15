export type User = {
  name: string;
  avatarUrl: string;
  isPro: boolean;
}

export type AuthorizationData = {
  login: string;
  password: string;
}

export type UserData = {
  email: string;
  token: string;
  name: string;
  avatarUrl: string;
  isPro: boolean;
}
